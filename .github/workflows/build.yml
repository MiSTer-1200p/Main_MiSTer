name: Build
on:
  push:
    branches: [master]
jobs:
  build:
    uses: MiSTer-1200p/Main_MiSTer/.github/workflows/build-feature.yml@master
    with:
      branch: ${{ github.ref_name }}
    secrets: inherit
